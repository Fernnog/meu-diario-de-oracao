case 'archive': {
    showConfirmationModal("Arquivar este alvo?", async () => {
        // ...lógica para arquivar...
        // (Coloque a lógica que estava dentro do `if` aqui)
    });
    break;
}